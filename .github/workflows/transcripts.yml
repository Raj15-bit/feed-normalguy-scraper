name: transcripts

# Fills the company "Documents" view for the last ~4 quarters across all 50:
#   1) backfill: fetch concall transcripts + investor presentations + annual
#      reports (incl. oddly-titled ones) over the FY window.
#   2) retitle: re-scan existing concall rows and re-title the ones whose
#      attached PDF is actually a transcript (e.g. filed as "Con. Call Updates").
# Runs weekly and on demand.

on:
  schedule:
    # Sunday 02:30 UTC (08:00 IST) — quiet window.
    - cron: "30 2 * * 0"
  workflow_dispatch:
    inputs:
      days:
        description: "Backfill window in days (last 4 quarters ≈ 400)"
        required: false
        default: "400"

concurrency:
  group: transcripts
  cancel-in-progress: false

jobs:
  transcripts:
    runs-on: ubuntu-latest
    timeout-minutes: 50
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
          cache: pip
      - name: Install tesseract (OCR fallback for scanned PDFs)
        run: sudo apt-get update && sudo apt-get install -y --no-install-recommends tesseract-ocr
      - run: pip install -r requirements.txt
      - name: Fetch transcripts + investor PPTs + annual reports (last 4 quarters)
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
          SCRAPER_MAX_FILINGS_PER_RUN: "10000"
        run: python -m scraper.backfill --days ${{ github.event.inputs.days || '400' }} --doc-types concall,investor_ppt,annual_report
      - name: Re-title hidden transcripts already in the DB
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
        run: python -m scraper.retitle_transcripts
